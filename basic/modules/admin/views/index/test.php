<?php
use kartik\date\DatePicker;
use app\models\Record;
use Yii;




/*echo DatePicker::widget([
    'name' => 'dp_2',
    'type' => DatePicker::TYPE_COMPONENT_PREPEND,
    'value' => '1980-01-05',
    'pluginOptions' => [
        'autoclose'=>true,
        'format' => 'yyyy-mm-dd'
    ]
]);

$rec = FastRec::find()->select('*')->orderBy(['id' => SORT_DESC])->one();
$dateTime = $rec->readDateTime();

//echo $fileName . '<br>';
var_dump($dateTime);
var_dump(date('m',strtotime('SEP')));

Yii::$app->formatter->locale = 'ru-RU'; 
echo Yii::$app->formatter->asDate(date('m'),"MMMM");*/
//var_dump($newPath);
//var_dump(Yii::$app->controller->module->id);

